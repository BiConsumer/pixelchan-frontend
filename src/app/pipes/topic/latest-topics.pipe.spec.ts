import { LatestTopicsPipe } from './latest-topics.pipe';

describe('LatestTopicsPipe', () => {
  it('create an instance', () => {
    const pipe = new LatestTopicsPipe();
    expect(pipe).toBeTruthy();
  });
});
